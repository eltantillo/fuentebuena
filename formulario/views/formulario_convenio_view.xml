<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <data>
        <record id="formulario_convenion_view_form" model="ir.ui.view">
            <field name="name">formulario.convenio.view.form</field>
            <field name="model">formulario.convenio</field>
            <field name="arch" type="xml">
                <form string="Formulario convenio">
                    <sheet>
                        <group>
                            <field name="name"/>
                            <field name="estado_id"/>
                            <field name="celula_id"/>
                            <field name="region_id"/>
                            <field name="estatus"/>
                        </group>
                    </sheet>
                </form>
            </field>
        </record>

        <record id="formulario_convenio_view_list" model="ir.ui.view">
            <field name="name">formulario.convenio.view.list</field>
            <field name="model">formulario.convenio</field>
            <field name="arch" type="xml">
                <list>
                    <field name="id"/>
                    <field name="name"/>
                    <field name="estado_id"/>
                    <field name="celula_id"/>
                    <field name="region_id"/>
                    <field name="estatus"/>
                </list>
            </field>
        </record>

        <record id="formulario_convenio_action" model="ir.actions.act_window">
            <field name="name">Convenios</field>
            <field name="res_model">formulario.convenio</field>
            <field name="view_mode">list,form</field>
            <field name="view_id" ref="formulario_convenio_view_list"/>
        </record>
    </data>
</odoo>